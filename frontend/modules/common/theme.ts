'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    secondary: {
      main: '#000',
    },
    primary: {
      main: '#000',
    },
  },
  typography: {},
  spacing: 8,
});

export default theme;
